[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cape"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "PyYAML>=6.0", "torch>=1.13", "traci", "sumolib"]

[tool.setuptools.packages.find]
where = ["src"]
